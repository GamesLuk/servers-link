<<<<<<< Updated upstream
# Server Instance Configuration
# This file defines the configuration for an individual server in the network
# Each server should have its own info.yml file with appropriate settings

# Group this server belongs to (defined in groups.yml)
# Determines which synchronization settings apply to this server
group: "global",

# Whether this server acts as a gateway/proxy server
# Gateway servers handle player connections and route them to other servers
gateway: false,

# IP address of the gateway server this instance connects to
# Use "127.0.0.1" for localhost or the actual gateway IP for remote connections
gateway-ip: "127.0.0.1",

# Port number of the gateway server for internal communication
gateway-port: 59001,

# Display name of this server in the network
# Shown to players when switching servers or in server lists
server-name: "Hub",

# IP address this server instance is running on
# Usually "127.0.0.1" for local servers or the server's public IP
server-ip: "127.0.0.1",

# Port number this Minecraft server is listening on
# Default Minecraft port is 25565
server-port: 25565,

# Command name players use to access network features
# Example: /network or /hub depending on this value
command-name: "network"
=======
group: global
gateway:
    gateway: false
    gateway-ip: 127.0.0.1
    gateway-port: 59001
server:
    server-name: Hub
    server-ip: 127.0.0.1
    server-port: 25565
gameplay:
    command-name: server
permissions:
    addOnJoin:
        - "x.x"
    addOnLeave:
        - "x.x"
    removeOnJoin:
        - "x.x"
    removeOnLeave:
        - "x.x"
>>>>>>> Stashed changes
